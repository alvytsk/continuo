//! Post-gain frequency spectrum: band geometry, the FFT analyzer, the output
//! tap, the transport mappings that label it, and the analysis worker (§10).

pub mod analyzer;
pub mod bands;
pub mod registry;
pub mod tap;
pub mod worker;
