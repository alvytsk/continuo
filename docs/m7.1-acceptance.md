# M7.1 acceptance

`docs/superpowers/specs/2026-09-18-tenuto-m7.1-radio-tab-design.md` §12 lists the validation. Test files are `tests/m7_1_*.rs`.

## Automated

| ID | Assertion (spec §12) | Tests |
| --- | --- | --- |
| V1 | The existing ICY station fixture gains `Icy-Genre` and `Icy-Logo`, and keeps asserting it sends no `icy-metaint` | `tests/http_server_selftest.rs::an_icy_station_sends_name_and_bitrate_but_no_metaint` still asserts no `icy-metaint` (unchanged). The fixture itself (`tests/support/server.rs`'s `icy_station()`/`icy_logo()`) now always sends `icy-genre` and sends `icy-logo` when set; that widening is exercised end to end, with an assertion on all four header-derived fields, by `tests/m7_1_station_probe.rs::a_live_station_is_saved_with_its_identity`. No test inside `http_server_selftest.rs` itself asserts on `icy-genre`/`icy-logo` at the raw-header level |
| V2 | **R2, non-negotiable:** no request Tenuto issues carries `Icy-MetaData` — initial open, reconnect, Pause → Play rejoin, station probe | `tests/m7_1_no_icy_metadata.rs::an_initial_open_never_asks_for_metadata`, `::a_reconnect_never_asks_for_metadata`, `::a_pause_then_play_rejoin_never_asks_for_metadata`, `::a_station_probe_never_asks_for_metadata` — all four situations, each reading the requests the `TestServer` actually received |
| V3 | `tests/m7_1_station_store.rs`: round-trip; a station with no identity; a malformed file quarantined to `stations.json.rejected-<stamp>` with the list starting empty; slug derivation and collision within the station namespace; a station slug colliding with a feed slug leaving both stores intact | `::a_verified_and_an_unverified_station_round_trip`, `::a_verified_but_entirely_unnamed_station_round_trips_as_verified` (round trip, including the unverified and the verified-but-unnamed cases); `::a_missing_file_is_an_empty_list_not_an_error`, `::read_snapshot_reports_a_malformed_file_and_never_quarantines_it`, `::load_quarantines_a_malformed_file_and_starts_empty` (quarantine to `stations.json.rejected-<stamp>`, list starts empty, confirmed against `src/station/store.rs`); `::a_duplicate_slug_makes_the_whole_file_malformed`, `::a_slug_is_derived_from_the_name_and_falls_back_to_the_host` (slug derivation and collision within the station namespace). **Gap:** no test anywhere in the suite has a station slug collide with a feed slug — that half of the bullet is unverified |
| V4 | `tests/m7_1_station_probe.rs`, against `TestServer` scripts: the five §10 rows, each on the store's contents and the notice text; identity from all four ICY headers; `icy-br`/`icy-logo` degrading to `None`; a re-probe of a now-finite URL; a duplicate URL | `::a_live_station_is_saved_with_its_identity` (Live → stored with identity), `::a_retryable_failure_saves_an_unverified_candidate` (retryable failure → stored, `identity: None`), `::a_non_retryable_failure_saves_nothing`, `::a_finite_url_is_not_a_station_and_is_not_saved`, `::an_icy_metaint_response_is_not_a_station_and_is_not_saved` (the two "not live" rows), `::a_malformed_url_makes_no_request` (validation failure), plus `::a_url_with_embedded_credentials_is_refused_and_never_echoed` and `::a_malformed_secret_bearing_url_is_refused_without_echoing_the_token` (the validation row's detail — review finding 2). `icy-br` non-decimal and `icy-logo` non-absolute degrading to `None`: `tests/m7_http_live.rs::a_relative_logo_and_a_non_numeric_bitrate_degrade_to_none` — this is not in `m7_1_station_probe.rs` as the spec bullet implies, but it is the real coverage. `::a_reprobe_of_a_now_finite_url_keeps_the_record_and_reports_the_failure`; `::a_duplicate_url_resolves_to_the_existing_station` and `::a_duplicate_add_whose_reprobe_fails_reports_the_reason_and_keeps_the_old_identity` (duplicate URL resolves to the existing station, including when its re-probe itself fails) |
| V5 | A station row's `MediaId` equals the one `EnqueueItem::Url` produces for the same URL, from the CLI path and from the Radio tab, one queue entry and a drawn tick | `tests/m5_browser.rs::a_station_queued_elsewhere_draws_a_tick_on_the_radio_tab`, which derives a `MediaId` through `resolve_source` (the CLI/enqueue path) and asserts it equals `StationRow::media` (the Radio tab's derivation, `station_identity_of`), for a URL `Url::parse` itself rewrites — then exercises the toggle: Enter on the resulting tick removes it |
| V6 | `tests/m5_browser.rs`: `Tab` cycles three tabs; `a`/`d`/`r` send the right requests and are refused while `pending`; Enter enqueues and re-Enter removes; a queued station draws a tick; an unverified row draws its URL and the unreached marker; a stale `Stations` answer is dropped | `::tab_cycles_files_podcasts_radio_and_back`; `::a_on_the_radio_tab_opens_the_url_prompt_and_enter_sends_add_station`, `::r_and_d_then_y_on_the_radio_tab_send_reprobe_and_remove`, `::every_station_management_key_is_refused_while_pending`, `::refresh_all_does_nothing_on_the_radio_tab` (`R` stays Podcasts-only); `::enter_on_a_station_enqueues_and_enter_again_removes_it`, `::enter_on_an_unqueued_station_enqueues_it`; `::a_queued_station_draws_a_tick`; `::a_stations_answer_for_a_tab_already_left_is_dropped`. **Gap:** no test asserts an unverified row's *drawn text* — its URL as the label and `(unreached)` as the marker. Every fixture station these tests use happens to carry `identity: None`, so the code path runs without panicking, but nothing checks the rendered label or marker text directly |
| V7 | `tests/m5_no_network.rs`: R3 — listing stations from a populated store keeps the server's request count at zero; `RemoveStation` builds no `HttpService` | `tests/m5_no_network.rs::a_restored_station_is_listed_and_drawn_without_a_request` now covers the listing half directly: a station already saved in the library is listed through `BrowseRequest::Stations` by a fresh `BrowseWorker`, and the fixture server's request count is asserted empty both before and after that read. `RemoveStation`'s zero-request guarantee is still not separately tested in this file; the only removal coverage remains `tests/m7_1_station_probe.rs::listing_and_removing_stations_open_no_connection`, which lists and removes a seeded station through a `BrowseWorker` and asserts the server's request count is unchanged across both operations combined — it does not isolate `RemoveStation`'s own contribution, nor assert that `RemoveStation` never builds an `HttpService` |
| V8 | §8.1, the artwork path, as an integration test: one `TestServer` serving a station and its logo, request counts per path for listing, enqueueing, restoring (zero), playing (exactly one), a non-station remote URL (embedded cover, no logo request), a station with no logo (embedded fallback) | `tests/m7_1_station_artwork.rs::listing_enqueueing_and_restoring_a_station_request_no_logo`, `::playing_a_station_requests_its_logo_exactly_once`, `::a_remote_url_no_station_claims_still_uses_its_embedded_cover`, `::a_station_with_no_logo_falls_back_to_the_embedded_cover`. Bonus, not named in the bullet: `::a_re_probed_logo_replaces_the_old_one_on_the_next_load`, pinning R4's artwork exception (review finding 6) |
| V9 | `tests/m7_1_artwork_svg.rs`: an SVG logo decodes to the expected raster; a 50000×50000 viewBox rasterizes within the fixed bound; a truncated and a malformed SVG each yield a typed error; detection is by content; a `.svgz` logo is rejected as unsupported | `::an_svg_logo_decodes_to_a_bounded_raster`, `::an_enormous_viewbox_still_rasterizes_within_the_bound`, `::aspect_ratio_is_kept_and_the_long_edge_hits_the_bound`, `::a_truncated_and_a_malformed_svg_are_typed_errors`, `::detection_is_by_content_not_by_declared_type`, `::a_gzipped_svg_is_unsupported_not_inflated`. Bonus: `::a_translucent_fill_keeps_its_straight_alpha_colour`, guarding the premultiplied-vs-straight-alpha regression (review finding 5) |
| V10 | Both `image_href_resolver` halves, separately: an external `href`/`resources_dir` resolves to nothing (`resolve_string`); a nested `data:image/svg+xml` **and** a `data:text/plain` payload whose magic bytes are not JPEG/PNG/GIF/WebP each resolve to nothing (`resolve_data`) | `::an_external_href_resolves_to_nothing` (`resolve_string`, external href and `resources_dir`); `::a_nested_data_svg_is_not_parsed` and `::a_nested_data_text_plain_payload_is_not_parsed` (`resolve_data`, both the `image/svg+xml` case and the `text/plain` fallback case asserted separately, as the spec requires) |
| V11 | `tests/m5_tui_render.rs`: the Radio tab's drawn overlay — tabs, rows, hints, empty-list text, prompt and confirmation | **Still not in `tests/m5_tui_render.rs`** — that file has no Radio/station reference at all (its one `_station_` hit, `a_reconnecting_station_says_so`, is the player's live-status line, unrelated to the browser overlay). The coverage lives in `tests/m5_browser.rs`'s own `screen()` helper instead: `::the_tab_bar_names_all_three_tabs` (the tab bar draws `" Files "`, `" Podcasts "` and `" Radio "`, checked on both the Files and the Radio tab); `::an_empty_radio_tab_says_so_and_the_hints_name_three_tabs` (the empty-list text, and the hints line naming `files/podcasts/radio`); `::the_radio_prompt_asks_for_a_stream_url` (the Radio tab's `a` prompt reads `Stream URL: `, Podcasts keeps `Feed URL: `); `::a_verified_row_draws_its_slug_then_genre_and_bitrate`, `::a_verified_row_with_only_a_bitrate_draws_just_the_bitrate` and `::a_verified_row_with_only_a_genre_draws_just_the_genre` (a verified row's slug, then genre and bitrate, each field's absence covered on its own — spec §7's own example, `Lofi · 128 kbps`, drawn whole and unclipped); `::an_unverified_station_draws_its_url_and_an_unreached_marker`; `::a_stations_identity_text_is_escaped` (hostile identity text escaped); `::a_queued_station_draws_a_tick` (a row and its tick). No test renders and asserts confirmation text (`Remove <slug>? y/N`) specifically for the Radio tab — that gap remains; the one screen test that checks prompt/confirm/notice text together, `::the_overlay_draws_the_prompt_the_question_and_the_notice_above_rows`, still runs only against the Podcasts tab. The tab-bar and stale-comment defects this row originally flagged are both fixed: `tabs()` now draws a `" Radio "` span, and the comment above `RADIO_HINTS` is a short, current one |

## Gates

`cargo fmt --check`, `cargo clippy --locked --all-targets --all-features -- -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc --locked --no-deps` — all exit 0, no warnings or errors. `cargo test --locked` — 96 binaries (95 test binaries plus the crate's doc-tests): 1178 passed, 0 failed, 1 ignored. The five `m7_1_*` suites: `m7_1_artwork_svg` 10 passed, `m7_1_no_icy_metadata` 4 passed, `m7_1_station_artwork` 5 passed, `m7_1_station_probe` 17 passed, `m7_1_station_store` 8 passed — 44 tests, all passing, 0 failed.

## Manual, Ghostty, live radio

Spec §12's closing paragraph, against `https://radio.cliamp.stream/lofi/stream` — confirmed to carry `Icy-Name: Lofi`, `Icy-Genre: Lofi`, `Icy-Br: 128` and `Icy-Logo: .../logo.svg`, and to send `Icy-Metaint` only when a client asks for it.

| Step | Result |
| --- | --- |
| `a`, paste the URL: `Lofi · 128 kbps` appears in the Radio tab | not run — requires a human at the terminal |
| Enter to enqueue, play: `LIVE`, listening time counting, logo in the cover pane | not run — requires a human at the terminal |
| Quit and relaunch: the station is listed, no connection opens until Play | not run — requires a human at the terminal |
| `r` re-probes and the row is unchanged | not run — requires a human at the terminal |
| `d` then `y` removes it | not run — requires a human at the terminal |
