# M7 acceptance

`docs/superpowers/specs/2026-09-17-tenuto-live-radio-design.md` §14 lists the evidence: L1–L20 against the local test server, then a manual run in Ghostty.

## Automated

| ID | Assertion | Tests |
| --- | --- | --- |
| L1 | An ICY-header `200` with no length loads as `Indefinite` / `Unsupported` and plays | `tests/http_response.rs::an_icy_200_at_origin_is_live_whatever_length_it_declares` (pure `accept()`); `tests/m7_http_live.rs::a_station_is_live_unsized_unseekable_and_named` (`HttpMediaSource::open`); `tests/prepare.rs::a_station_prepares_as_indefinite_and_unseekable_with_its_name_as_title` (`prepare()`); `tests/m7_live_playback.rs::a_station_loads_fresh_at_zero_as_indefinite_and_plays` (end to end) |
| L2 | The same headers plus `Content-Length`, or a `206` starting at zero, still classify as live | `tests/http_response.rs::an_icy_200_at_origin_is_live_whatever_length_it_declares` (`Content-Length` ignored); `tests/http_response.rs::an_icy_206_from_zero_is_live_and_from_anywhere_else_is_a_wrong_start`. The `206`-with-ICY half is covered by the pure `accept()` test only — `TestServer`'s `write_206` cannot carry ICY headers, a limit recorded in `docs/m1-known-debt.md` (M3 carried debt) |
| L3 | `icy-metaint` → `IcyFramingUnsupported`; HLS content type → `UnsupportedLiveMedia` | `tests/http_response.rs::metadata_framing_is_refused_until_it_can_be_demultiplexed`; `tests/http_response.rs::an_hls_playlist_is_refused_before_any_probe` |
| L4 | ICY headers on `404`, `429`, `503` yield `Status` failures; `is_retryable` false, true, true; initial open all three `Failed` with one request | `tests/http_response.rs::an_error_status_is_never_live_whatever_icy_headers_it_carries`; `tests/http_errors.rs::only_failures_that_say_nothing_about_the_location_are_retryable`; `tests/m7_reconnect.rs::an_initial_open_that_fails_is_not_retried`; `tests/m7_reconnect.rs::a_gone_station_fails_on_the_first_reconnect` |
| L5 | Finite file, finite chunked body and unresolved source behave as before (existing H-suite unchanged) | No new test; the pre-existing finite-media cases in `tests/http_response.rs`, `tests/http_playback.rs` and `tests/prepare.rs` keep passing under the same `cargo test --locked` run |
| L6 | Server closes mid-stream: `Reconnecting`, then `Playing`; no `EndOfTrack`, `Ended`, `completed` or `Advance` | `tests/m7_reconnect.rs::a_disconnect_reconnects_and_never_ends_the_track` |
| L7 | Listening time may advance after the disconnect while buffered audio plays, by at most the ring's content; it is then constant until step 7; the new generation is anchored at the captured value | `tests/m7_reconnect.rs::listening_time_counts_drained_audio_and_then_stands_still`. The "by at most the ring's content" upper bound is not asserted — the test checks only a lower bound (at least 5 of the ~6 buffered seconds are counted) and that the position then stands still while disconnected |
| L8 | Cancellation, for each of Pause, Stop, replacement Load and Shutdown, in each of five situations — waiting out backoff, waiting for headers, a stalled body read, a delivering body read, decoder priming: the command's own transition is observed within the handshake deadline, no `Playing` is announced, and no further request for the cancelled operation is issued | `tests/m7_cancellation.rs`, the 20-cell matrix: `backoff_{pause,stop,load,shutdown}`, `headers_{pause,stop,load,shutdown}`, `stalled_{pause,stop,load,shutdown}`, `delivering_{pause,stop,load,shutdown}`, `priming_{pause,stop,load,shutdown}` |
| L9 | `503` on every reconnect → `Failed` when the injected budget expires, with `cause` the last `Status`; a following `Play` makes exactly one attempt. `404` on the first reconnect → `Failed` at once, one request | `tests/m7_reconnect.rs::a_retryable_refusal_exhausts_the_budget_and_play_then_tries_exactly_once`; `tests/m7_reconnect.rs::a_gone_station_fails_on_the_first_reconnect` |
| L10 | A server that accepts and closes after 2 s, repeatedly, exhausts the budget; 30 s of played audio (injected clock) clears the outage; an in-flight open is not cut short by the budget | `tests/m7_reconnect.rs::a_server_that_keeps_closing_early_is_one_outage`; `tests/m7_reconnect.rs::sustained_playback_ends_the_outage_so_a_later_drop_gets_a_fresh_budget`; `tests/m7_reconnect.rs::a_failure_during_an_outage_leaves_no_budget_behind_for_the_next_one`; the pure policy is unit-tested in `src/playback/reconnect.rs`: `backoff_steps_then_repeats_its_last_entry`, `the_budget_is_judged_only_when_something_fails`, `an_attempt_is_due_at_its_scheduled_instant_and_not_a_moment_before`, `short_connections_stay_one_outage_and_thirty_played_seconds_end_it` |
| L11 | Initial open failure is `Failed` with no retry | `tests/m7_reconnect.rs::an_initial_open_that_fails_is_not_retried` |
| L12 | Pause: the server observes the close — also when the body read is stalled; the ring is discarded; Play issues a new request at byte zero; no pre-pause audio is played; `Playing` is announced only after priming. Thaw after a hook-parked pause announces nothing | `tests/m7_live_recovery.rs::pause_closes_the_connection_and_play_rejoins_with_listening_time_kept`; `tests/m7_live_recovery.rs::pause_wakes_a_stalled_live_read_and_thaw_announces_nothing` |
| L13 | No seek: across Stop → Play, Pause → Play, reconnect and device-fault `rebuild` on a live source, no `Range` start above zero and no `SeekCompleted`/`RestartEstablished` | `tests/m7_live_recovery.rs::stop_then_play_reopens_without_a_seek`, `::pause_closes_the_connection_and_play_rejoins_with_listening_time_kept`, `::a_device_fault_recovers_a_station_without_seeking` all call the file's `no_range_above_zero` helper against every request the server saw. `tests/m7_reconnect.rs::a_disconnect_reconnects_and_never_ends_the_track` covers the reconnect leg only by request count and the absence of end-of-track events — it does not call an equivalent range check, so that leg's "no `Range` start above zero" half is not directly asserted. No counting `MediaSource` was added; the assertion is at the wire throughout, as the spec's own note anticipates |
| L14 | Rejected commands are harmless: `submit_seek`, a `SeekBy` burst through `KeyRouter`, and `Restart`, through the public submission paths, each yield `SeekRejected`; audio continues, the server sees no new request, the state and the outage fields are unchanged, no `RestartEstablished`. The `submit_seek` half is repeated for a finite source on a server without ranges | `tests/m7_live_playback.rs::seeks_and_restart_are_rejected_and_the_stream_is_untouched`; `tests/m7_submission.rs::a_rejected_seek_leaves_a_range_less_stream_playing_on_its_one_connection` |
| L15 | Reconnect to a decoder with a different sample rate, and with a different channel count, plays correctly. A source that probes but fails while priming is a failed attempt: no `Playing`, next attempt scheduled | `tests/m7_reconnect.rs::a_reconnect_to_different_audio_parameters_plays`; `tests/m7_reconnect.rs::a_source_that_probes_but_dies_while_priming_is_a_failed_attempt` |
| L16 | Checkpoints: none for a station on tick, pause, stop, track change or shutdown. Finite → live writes the outgoing finite checkpoint; live → finite writes none for the station; same-URL finite → live leaves the old checkpoint byte-identical | `tests/m7_session.rs::a_station_is_never_checkpointed_by_tick_pause_stop_or_shutdown`, `::loading_a_station_still_checkpoints_the_finite_track_on_its_way_out`, `::loading_a_finite_track_writes_nothing_for_the_station_on_its_way_out`, `::a_url_that_was_finite_and_is_now_live_keeps_its_old_checkpoint_untouched` |
| L17 | Continuity mismatch on each reopen path — reconnect, Pause → Play, Stop → Play, `Failed` → Play — fails with `ResourceChanged`; no `CapabilitiesChanged` with `Finite` is emitted, no checkpoint is written, listening time is retained. The symmetric finite → live reopen fails the same way | `tests/m7_live_recovery.rs::a_station_url_that_turns_finite_fails_every_play_path_without_leaking_finite_capabilities` (end to end, one session, every reopen path); `tests/prepare.rs::a_reopen_that_changes_continuity_is_resource_changed_in_both_directions` (pure `prepare()`, both directions) |
| L18 | Space during `Reconnecting` pauses, through `PlayerRuntime` and through the direct-engine `TogglePause`; an explicit `Play` during `Reconnecting` changes nothing and does not reset the budget | `tests/m7_reconnect.rs::toggle_pauses_a_reconnect_and_play_during_one_changes_nothing` (direct engine); `tests/m7_runtime.rs::space_during_a_reconnect_pauses_and_arrow_keys_never_reach_the_engine` (through `PlayerRuntime`); `tests/m5_transport_rules.rs::reconnecting_is_controlled_like_playing` (pure `transport::decide`) |
| L19 | A plain `Play` from the post-load `Paused` on a station takes a fresh open; `PlayLoaded` releases the primed transport | `tests/m7_live_recovery.rs::a_plain_play_after_load_opens_fresh_but_play_loaded_releases_the_primed_transport` |
| L20 | Process restart with a station active: entry restored, zero requests until Play | `tests/m5_no_network.rs::a_restored_active_station_is_listed_and_drawn_without_a_request` |

Front-end rendering, not tied to one L-number but exercised alongside them (spec §10, §14's status-line rule):

- `tests/m5_tui_render.rs::a_live_entry_shows_live_and_listening_time_with_no_bar_or_duration`, `::a_reconnecting_station_says_so` — TUI drawing.
- `tests/m5_transport_rules.rs::a_live_entry_answers_every_seek_and_home_with_a_notice` — `transport::decide` returns a notice instead of submitting a seek.
- `tests/http_cli.rs::probe_only_reports_a_station_as_indefinite_and_exits_zero` — `play --probe-only` prints `continuity=Indefinite` (the same `key=Debug` spelling `probe_only` already used for `continuity=Finite`) and exits zero.
- `src/app.rs::tests::a_live_source_reads_live_not_as_a_duration` — the plain `play` status line shows `live` in place of a duration.

Other HTTP-layer tests exercised alongside the L-suite: `tests/http_server_selftest.rs::an_icy_station_sends_name_and_bitrate_but_no_metaint`, `::an_icy_station_answers_a_range_request_with_a_plain_200`, `::a_script_chain_serves_each_connection_its_own_script_and_repeats_the_last` (the test server's own live-station fixture behaves as documented).

## Gates

`cargo fmt --check`, `cargo clippy --locked --all-targets --all-features -- -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc --locked --no-deps` — all exit 0, no output. `cargo test --locked` — 90 binaries: 1114 passed, 0 failed, 1 ignored.

## Manual, Ghostty, live radio

Spec §14's closing paragraph, against the verified endpoint (`https://radio.cliamp.stream/lofi/stream`), from the release build.

| Step | Result |
| --- | --- |
| Play the verified endpoint in Ghostty from the release build: `LIVE`, listening time counting up, no bar | not run — requires a human at the terminal |
| Pull the network for 20 s: `reconnecting…`, then playback resumes | not run — requires a human at the terminal |
| Pull the network for 6 min: reconnect gives up, state shows `Failed`; Space tries exactly once more | not run — requires a human at the terminal |
| Pause for 2 min and resume: connection closed while paused, rejoins the live edge on Play | not run — requires a human at the terminal |
| Hammer the arrow keys while playing: every seek is rejected with a notice, audio is undisturbed | not run — requires a human at the terminal |
| Quit and relaunch: the station is restored, no connection opens until Play | not run — requires a human at the terminal |
